# Portfólio — Netto

Site pessoal/portfólio. HTML + CSS puro, zero dependências de build.

## Deploy (Cloudflare Pages)
- Build command: *(nenhum)*
- Output directory: `/` (raiz)

Estrutura:
- `index.html` — o site inteiro
- `assets/` — imagens
